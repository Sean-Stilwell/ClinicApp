<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.clinicprojectv2">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Employee.EmployeeActivity"></activity>
        <activity
            android:name=".Administrator.AdminMainActivities.AdminActivity"
            android:label="@string/title_activity_admin" />
        <activity
            android:name=".Employee.WorkingHoursActivity"
            android:label="@string/title_clinic_hours"
            android:exported="true"></activity>
        <activity android:name=".CreateAccount" />
        <activity android:name=".WelcomeScreen" />
        <activity android:name=".LoginScreen" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".RateApp" />
    </application>

</manifest>